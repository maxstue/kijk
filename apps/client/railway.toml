[build]
builder = "RAILPACK"
buildCommand = "pnpm build:client"
watchPatterns = [
  "apps/client/**",
  "packages/**",
  "package.json",
  "pnpm-lock.yaml",
  "pnpm-workspace.yaml",
  "oxfmt.config.ts",
]

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
